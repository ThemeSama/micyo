import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as usePosts from './usePosts.stories';

<Meta of={usePosts} title="react-wp-query/usePosts" />

# usePosts

Fetches the latest posts published on WordPress. The list can be narrowed down based on specific criteria, and the REST API result can be customized. Details of a single post can also be queried. For references and parameters, visit https://developer.wordpress.org/rest-api/reference/posts/.

<Canvas of={usePosts.Posts} />
